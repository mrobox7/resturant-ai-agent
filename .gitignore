# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets. The glob also covers .env.bak, .env.local and friends — a
# bare `.env` line does not, and those hold real keys too.
.env
.env.*
!.env.example
