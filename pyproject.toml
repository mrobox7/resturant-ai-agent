[project]
name = "ai-agent"
version = "0.1.0"
description = "mock ai agent"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115",
    "openai>=1.51",
    "pydantic>=2.9",
    "pydantic-settings>=2.5",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]
